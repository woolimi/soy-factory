# SoyDB — MySQL 8. 루트 docker-compose에서 사용.
# 빌드: 프로젝트 루트에서 docker compose build mysql
FROM mysql:8

# 기본 환경변수 (docker-compose에서 오버라이드 가능)
ENV MYSQL_ROOT_PASSWORD=soyroot
ENV MYSQL_DATABASE=soydb
ENV MYSQL_USER=soy
ENV MYSQL_PASSWORD=soy
